# Metro (React Native's bundler) does not follow pnpm's symlinked store reliably.
# A hoisted node_modules layout keeps Expo/Metro resolution working in the monorepo.
node-linker=hoisted
# Bleeding-edge deps in this repo have noisy peer ranges; don't fail install on them.
strict-peer-dependencies=false
auto-install-peers=true
